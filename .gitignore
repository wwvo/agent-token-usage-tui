# Rust build artifacts
/target/
**/*.rs.bk
Cargo.lock.bak

# Portable runtime artifacts (written next to the exe)
*.db
*.db-wal
*.db-shm
*.log
/log/
pricing.json

# build.rs downloaded asset (regenerated every build)
/assets/litellm-prices.fallback.json

# git-cliff preview output (CHANGELOG.md is hand-curated; this file is
# a throw-away dry run, e.g. `git-cliff -o CHANGELOG.auto.md`).
CHANGELOG.auto.md
release-notes.md

# Editors / IDEs
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db

# Environment
.env
.envrc

# OS-specific junk
ehthumbs.db
